import { Typography } from "@mui/material";

export const Hero: React.FC = () => {
  return (
    <a
      href="https://github.com/HarukiFT"
      style={{
        textDecoration: "none",
        color: "black",
      }}
    >
      <Typography variant="h5">Rostic's</Typography>
    </a>
  );
};
